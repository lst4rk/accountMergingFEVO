// imports json data from accounts.json
const accountsData = require('./accounts.json');

// initial check for correctly imported data
console.log(accountsData);

// To Do: create listing  of people from data

// find identical emails
    // merge emails -> [string]
    // merge applications -> [string]
    // if names differ, pick one -> string